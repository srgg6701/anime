// ParsleyConfig definition if not already set
window.ParsleyConfig = window.ParsleyConfig || {};
window.ParsleyConfig.i18n = window.ParsleyConfig.i18n || {};

// Define then the messages
window.ParsleyConfig.i18n.en = $.extend(window.ParsleyConfig.i18n.en || {}, {
  defaultMessage: globals.parsley.defaultMessage,
  type: {
    email:        globals.parsley.email,
    url:          globals.parsley.url,
    number:       globals.parsley.number,
    integer:      globals.parsley.integer,
    digits:       globals.parsley.digits,
    alphanum:     globals.parsley.alphanum
  },
  notblank:       globals.parsley.notblank,
  required:       globals.parsley.required,
  pattern:        globals.parsley.pattern,
  min:            globals.parsley.min,
  max:            globals.parsley.max,
  range:          globals.parsley.range,
  minlength:      globals.parsley.minlength,
  maxlength:      globals.parsley.maxlength,
  length:         globals.parsley.length,
  mincheck:       globals.parsley.mincheck,
  maxcheck:       globals.parsley.maxcheck,
  check:          globals.parsley.check,
  equalto:        globals.parsley.equalto
});

// If file is loaded after Parsley main file, auto-load locale
if ('undefined' !== typeof window.ParsleyValidator)
  window.Parsley.addCatalog('en', window.ParsleyConfig.i18n.en, true);